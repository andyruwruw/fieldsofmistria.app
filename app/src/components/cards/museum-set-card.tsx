/* eslint-disable @typescript-eslint/no-explicit-any */
// Types
import type { MuseumSet } from '../../types/museum';

/**
 * Props for the MuseumSet component.
 */
interface MuseumSetProps {
  set: MuseumSet;
}

/**
 * MuseumSet component that displays a character's information.
 * @param props The props for the component.
 * @returns The rendered component.
 */
export const MuseumSetCard = ({
	set,
}: MuseumSetProps) => {
  return (

  );
}