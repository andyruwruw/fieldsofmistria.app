{
  "name": "fieldsofmistria.app-workspace",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "concurrently -n server,app -c blue,green \"npm run start --prefix server\" \"npm run dev --prefix app\""
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
